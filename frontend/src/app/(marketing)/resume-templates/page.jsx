import Link from 'next/link';
import { ArrowRight, FileText } from 'lucide-react';
const groups = ['One page', 'Two page', 'Student', 'Entry level', 'Senior level'];
export default function ResumeTemplatesPage() { return <div className="product-page"><section className="product-hero compact"><div><p className="landing-kicker">Resume templates</p><h1>A clear starting point for every draft.</h1><p>Use an ATS-friendly structure, then edit it in the builder. The layout is a starting point, not a substitute for evidence.</p><Link href="/builder" className="btn btn-primary">Build a resume <ArrowRight size={15} /></Link></div></section><section className="product-section"><div className="filter-line">{groups.map(group => <button type="button" key={group}>{group}</button>)}</div><div className="template-gallery">{['Classic', 'Relay', 'Ledger', 'Arc', 'Signal', 'Plain text'].map((name, index) => <Link href="/builder" className="gallery-item" key={name}><div className={`gallery-paper template-${index}`}><FileText size={34} /><span /><span /><span /><span /></div><h3>{name}</h3><p>ATS-friendly structure</p></Link>)}</div></section></div>; }
