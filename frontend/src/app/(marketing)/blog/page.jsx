import Link from 'next/link';
import { ArrowRight, BookOpen } from 'lucide-react';
const articles = [{ title: 'What an ATS can and cannot read', type: 'Parsing', text: 'A practical look at sections, columns, images, and the text layer in a resume.' }, { title: 'How to write evidence instead of duties', type: 'Content impact', text: 'Use the result, scale, and action behind the work you already did.' }, { title: 'When keyword matching is not measured', type: 'Method', text: 'Why a missing job description should stay visible in the score.' }];
export default function BlogPage() { return <div className="product-page"><section className="product-hero compact"><div><p className="landing-kicker">ResumeIQ / Notes</p><h1>Guides for clearer applications.</h1><p>Short, practical explanations of resume structure, evidence, ATS behavior, and the method behind the score.</p></div></section><section className="article-list">{articles.map(article => <article key={article.title}><BookOpen size={21} /><small>{article.type}</small><h2>{article.title}</h2><p>{article.text}</p><Link href="/method" className="text-link">Read the method <ArrowRight size={14} /></Link></article>)}</section></div>; }
